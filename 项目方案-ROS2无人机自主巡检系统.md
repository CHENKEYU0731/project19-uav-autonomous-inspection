# 项目方案：基于 PX4 + ROS2 的无人机自主巡检系统（仿真优先）

> 本文档是项目的**总目标文档**，供 Codex（或其他 AI 编程助手）作为长期目标持续推进。
> 使用方式：将本文档放在项目仓库根目录，让 Codex 每次工作前先阅读本文档，按里程碑逐步推进，每个里程碑必须通过"验收标准"才能进入下一个。
> **工作文件存放约束**：项目执行过程中生成的源代码、构建产物、日志、缓存、下载文件和临时文件，除操作系统或工具明确要求必须写入 C 盘外，均应优先存放在当前项目工作文件夹或其子目录中；不得无必要占用 C 盘。当前机器的大容量 WSL VHD 作为明确例外存放在 `D:\codex-wsl\project19-Ubuntu-22.04`，不得迁回 C 盘。

---

## 一、项目定位与简历价值

### 1.1 一句话描述

构建一套完整的无人机自主导航系统：无人机在 Gazebo 仿真环境中，携带深度相机，在有障碍物的未知场景中**自主起飞 → 感知建图 → 规划避障 → 巡检指定航点 → 自动返航降落**，全程无人工干预。

### 1.2 为什么选这个项目（决策依据）

- **主攻无人机方向**：PX4 + ROS2 是无人机行业的事实标准技术栈（大疆车载、亿航、极飞、美团无人机、丰翼科技等公司的岗位 JD 中高频出现）。
- **可迁移到智能驾驶**：项目中的核心模块——感知建图、路径规划、运动控制、系统集成——与智驾岗位的"规划控制（PNC）"岗位技能高度重合。面试时可以直接讲 A*/RRT、轨迹优化、状态机，这些是智驾 PNC 面试的核心考点。
- **可迁移到机器人研发**：ROS2 节点设计、TF 坐标变换、传感器数据处理、Gazebo 仿真，是所有机器人公司研发岗的通用基础。
- **与你的专业匹配**：控制科学与工程专业背景 + 无人机控制项目 = 自洽的简历故事线。
- **利用现有资源**：导师有一台自研无人机，项目采用"仿真优先"设计，后期如有机会可将 Offboard 控制模块部署到真机（真机部分为可选加分项，不是必须）。
- **1-2 个月业余时间可完成**：方案已按此约束裁剪，核心亮点集中，不贪多。

### 1.3 简历上的呈现（项目完成后可直接使用的模板）

> **基于 PX4/ROS2 的无人机自主巡检系统**（个人项目）
> - 基于 ROS2 Humble + PX4 + Gazebo 搭建无人机软件在环（SITL）仿真系统，通过 uXRCE-DDS 实现 ROS2 与飞控的通信，开发 Offboard 模式下的航点飞行控制节点；
> - 基于深度相机点云构建局部占据栅格地图，实现 A* 全局路径规划与安全走廊约束下的轨迹平滑，无人机可在含障碍物的未知环境中自主避障飞行；
> - 设计基于行为树/有限状态机的任务管理器，实现"起飞—巡检—异常处理—返航降落"全流程自主执行，支持任务中断恢复；
> - 使用 Docker 封装开发环境，配置 GitHub Actions CI 自动构建与测试，编写完整的中英文文档与演示视频。

---

## 二、技术选型（明确版本，避免踩坑）

| 组件 | 选型 | 版本/说明 |
|---|---|---|
| 操作系统 | Ubuntu | 22.04 LTS |
| ROS2 | Humble Hawksbill | LTS，行业主流，与 Ubuntu 22.04 配套 |
| 飞控 | PX4 Autopilot | v1.14+（SITL 软件在环仿真） |
| 仿真器 | Gazebo（新版，原 Ignition） | Harmonic（PX4 官方当前推荐；Humble 下需安装 ros-humble-ros-gzharmonic 桥接包，勿装默认的 Fortress 版 ros-gz） |
| ROS2-PX4 通信 | uXRCE-DDS + px4_ros_com / px4_msgs | PX4 官方推荐方案（替代旧的 MAVROS 路线） |
| 感知传感器 | 仿真深度相机 | Gazebo 内置 depth camera 插件 |
| 建图 | 局部占据栅格 / OctoMap | 二选一，优先自研轻量局部栅格（更能体现能力） |
| 规划 | A*（全局）+ 轨迹平滑（B样条或minimum-snap简化版） | 自研实现为主，体现算法能力 |
| 任务管理 | 有限状态机（可升级为行为树 BehaviorTree.CPP） | 先 FSM，时间充裕再升级 |
| 可视化 | RViz2 + PlotJuggler | 调试与演示 |
| 工程化 | Docker + docker-compose + GitHub Actions | 简历加分项 |
| 语言 | C++（核心节点）+ Python（工具脚本） | JD 中 C++ 是硬要求，核心模块必须用 C++ |

**重要提示（给 Codex）**：PX4 与 Gazebo 的版本配套关系变化较快，环境搭建阶段必须先查阅 PX4 官方文档（docs.px4.io）确认当前推荐的版本组合，不要凭训练数据中的旧知识安装。备选组合：若从全新系统开始，也可选 Ubuntu 24.04 + ROS2 Jazzy + Gazebo Harmonic（PX4 官方对新用户的建议之一），但社区资料以 Humble 更多，本方案默认 Humble。

---

## 三、系统架构

```
┌─────────────────────────────────────────────────────┐
│                    Gazebo 仿真环境                    │
│   （无人机模型 + 深度相机 + 障碍物场景 world 文件）      │
└───────────────┬─────────────────────┬───────────────┘
                │                     │
        PX4 SITL 飞控            深度相机数据
                │                     │
         uXRCE-DDS Agent        /camera/depth
                │                     │
┌───────────────▼─────────────────────▼───────────────┐
│                     ROS2 应用层                       │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ 感知建图  │→ │ 规划模块  │→ │ Offboard 控制节点 │   │
│  │ mapping  │  │ planner  │  │   controller     │   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
│        ↑             ↑                ↑             │
│  ┌─────┴─────────────┴────────────────┴──────────┐  │
│  │            任务管理器 mission_manager           │  │
│  │        （FSM：起飞/巡检/避障/异常/返航）          │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

ROS2 功能包划分（monorepo，一个 colcon 工作空间）：

- `drone_bringup`：launch 文件、参数配置、仿真场景启动
- `drone_perception`：深度图 → 点云 → 局部占据栅格地图
- `drone_planner`：A* 全局规划 + 轨迹平滑 + 重规划逻辑
- `drone_controller`：Offboard 控制、航点跟踪、速度/位置指令下发
- `drone_mission`：任务状态机、航点管理、异常处理（如低电量模拟、目标不可达）
- `drone_interfaces`：自定义 msg/srv/action 定义
- `drone_sim`：Gazebo world 文件、无人机模型配置、障碍物场景

---

## 四、里程碑计划（8 周，业余时间，每周约 10-15 小时）

> 每个里程碑都有明确的**验收标准**。Codex 必须在验收标准全部通过后才能进入下一个里程碑。每完成一个里程碑，打一个 git tag（如 `v0.1-m1`），并录制一段短视频/GIF 存档。

### M0：环境搭建与跑通官方例程（第 1 周）

任务：
1. 安装 ROS2 Humble、PX4 SITL、配套版本的 Gazebo、uXRCE-DDS Agent
2. 跑通 PX4 官方 SITL 仿真：命令行起飞/降落
3. 跑通 px4_ros_com 官方 offboard 控制例程（C++）
4. 初始化 git 仓库，建立 colcon 工作空间目录结构，编写 AGENTS.md

验收标准：
- [x] `make px4_sitl gz_x500` 能启动仿真并在 QGroundControl 或命令行中起飞
- [x] ROS2 侧能 `ros2 topic echo /fmu/out/vehicle_odometry` 收到飞控里程计数据
- [x] 官方 offboard 例程能让无人机起飞到指定高度并悬停
- [x] 仓库有清晰的 README（含环境搭建步骤，能从零复现）

### M1：Offboard 航点飞行（第 2 周）

任务：
1. 开发 `drone_controller` 节点：起飞 → 依次飞过一串预设航点 → 返航 → 降落
2. 实现航点到达判断（位置误差阈值 + 稳定时间）
3. 用 launch 文件一键启动整个流程
4. 用 PlotJuggler 绘制期望轨迹 vs 实际轨迹曲线（放进 README）

验收标准：
- [x] 一条命令启动后，无人机全自主完成"起飞→4 个以上航点→降落"，无人工干预
- [x] 航点跟踪误差有量化记录（如平均误差 < 0.3 m）
- [x] 代码通过 colcon build 无警告，节点有单元测试（至少覆盖航点判断逻辑）

### M2：感知与局部建图（第 3-4 周）

任务：
1. 在 Gazebo 无人机模型上挂载深度相机，搭建含障碍物（柱子、墙体、门框）的巡检场景 world
2. 开发 `drone_perception` 节点：深度图 → 点云 → 机体周围局部占据栅格地图（3D 体素或 2.5D 均可，推荐先做以固定飞行高度切片的 2D 占据栅格，简单可靠）
3. 正确处理 TF：相机系 → 机体系 → 世界系
4. RViz2 中实时可视化占据栅格与无人机位姿

验收标准：
- [x] 无人机悬停时，RViz2 中能看到与 Gazebo 场景一致的障碍物栅格
- [x] 无人机移动时地图随之更新，无明显错位（TF 正确）
- [x] 建图节点处理频率 ≥ 5 Hz，有延迟统计

### M3：路径规划与自主避障（第 5-6 周，核心亮点）

任务：
1. 开发 `drone_planner` 节点：在占据栅格上实现 A* 全局规划（含障碍物膨胀）
2. 轨迹平滑：将 A* 折线路径平滑为可飞轨迹（B 样条或分段多项式，控制最大速度/加速度）
3. 重规划逻辑：飞行途中发现新障碍物遮挡当前路径时，自动触发重规划
4. 与 M1 的控制节点打通：规划输出 → 轨迹跟踪

验收标准：
- [x] 给定起点和终点（中间有障碍物），无人机能自主绕障到达，全程不碰撞
- [x] 在飞行途中动态加入新障碍物（Gazebo 中手动放置），无人机能重规划绕行
- [x] 连续 10 次随机场景测试，成功率 ≥ 8/10，有测试记录表格

### M4：任务状态机与完整巡检演示（第 7 周）

任务：
1. 开发 `drone_mission` 节点：FSM 管理"待命→起飞→巡检（多航点+避障）→异常处理→返航→降落"
2. 异常处理至少实现两种：目标点不可达（跳过并告警）、模拟低电量（立即返航）
3. 制作最终演示场景：一个类似"变电站/仓库巡检"的完整 world，无人机绕场巡检一圈
4. 录制完整演示视频（Gazebo + RViz2 双画面）

验收标准：
- [x] 一条命令完成全流程巡检演示，含至少一次避障和一次异常处理
- [x] 状态机有日志输出，事后可用日志复盘每次状态切换
- [x] 演示视频 ≤ 3 分钟，可放简历/面试展示

### M5：工程化收尾（第 8 周）

任务：
1. Docker 化：提供 Dockerfile + docker-compose，一键拉起完整仿真环境
2. GitHub Actions CI：自动 colcon build + 单元测试 + lint（ament_lint）
3. 完善文档：中英文 README、架构图、各模块设计说明、复现指南
4. （可选加分）性能分析报告：规划耗时、建图延迟、CPU 占用

验收标准：
- [ ] 在一台全新机器上，按 README 用 Docker 能在 30 分钟内复现演示
- [ ] CI 全绿，main 分支受保护
- [x] 仓库整洁：无大文件、无临时文件、commit 历史清晰

### M6（可选，时间充裕或后续假期）：真机部署预留

- 将 `drone_controller` 的 Offboard 模块适配导师的真机（需确认真机飞控是否为 PX4）
- 先做 HITL（硬件在环）验证，再在有安全绳/网的条件下试飞
- **安全第一：真机试飞必须有导师或有经验的同学在场，此部分不写入 8 周计划**

---

## 五、如何让 Codex 持续推进本项目

### 5.1 仓库根目录放置 AGENTS.md，内容要点

```markdown
# AGENTS.md（示例骨架，M0 阶段由 Codex 细化）
- 项目总目标见《项目方案-ROS2无人机自主巡检系统.md》，严格按里程碑推进
- 当前里程碑：M5（每完成一个更新此行）
- 技术栈：ROS2 Humble / PX4 v1.14+ / Gazebo Harmonic / C++17
- 构建命令：colcon build --symlink-install
- 测试命令：colcon test
- 编码规范：ament_lint 默认规则；核心逻辑必须有单元测试
- 禁止事项：不要更换技术栈；不要跳过验收标准；版本问题先查 PX4 官方文档
```

### 5.2 与 Codex 协作的节奏建议

1. **每次会话开头**：让 Codex 读方案文档 + AGENTS.md，报告当前里程碑进度，再开始干活
2. **小步提交**：每完成一个可验证的小功能就 commit，commit message 用英文规范格式
3. **你自己必须做的事**（否则面试会露馅）：
   - 每个里程碑完成后，让 Codex 给你讲解本阶段代码的设计思路，你能不看代码复述出来
   - 亲手跑通每个演示，亲手改几个参数观察效果（如膨胀半径、飞行速度）
   - 面试高频问题提前准备：A* 与 Dijkstra/RRT 的区别、为什么要轨迹平滑、TF 树怎么组织的、ROS2 的 QoS 是什么、Offboard 模式的心跳机制
4. **卡住超过 2 小时的问题**：让 Codex 联网搜 PX4 官方论坛/GitHub Issues，版本兼容问题占坑的 80%

### 5.3 风险与备选方案

| 风险 | 应对 |
|---|---|
| PX4 + 新版 Gazebo 环境搭建踩坑（最大风险） | 预留整整一周；若卡死，降级用 Gazebo Classic + PX4 v1.13 的成熟组合 |
| uXRCE-DDS 例程跑不通 | 备选 MAVROS 路线（生态更老但资料多），架构不变 |
| 8 周内做不完 M4 | M3 完成即可写简历（自主避障已是完整亮点），M4/M5 顺延 |
| 独显驱动/Gazebo 渲染问题 | Gazebo 可用无 GPU 渲染模式跑逻辑，RViz2 可视化不受影响 |

---

## 六、方向迁移说明（面试时的讲法）

- **投无人机岗**：完整讲整个项目，重点讲 PX4 通信机制、Offboard 控制、规划避障
- **投智驾 PNC 岗**：重点讲 A* + 轨迹平滑 + 重规划（对应智驾的全局/局部规划），占据栅格建图（对应 freespace），FSM 任务管理（对应行为规划）；说明"无人机是 3D 空间的规划控制，比车辆 2D 更复杂"
- **投机器人研发岗**：重点讲 ROS2 系统设计（节点拆分、QoS、TF、launch 体系）、仿真测试体系、Docker/CI 工程化

祝项目顺利。完成后这将是一个"讲得清、跑得通、复现得了"的高质量简历项目。
