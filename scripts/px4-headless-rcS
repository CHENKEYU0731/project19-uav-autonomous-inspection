#!/bin/sh

. etc/init.d-posix/rcS

# The x500 airframe enables GCS data-link loss after environment overrides run.
# Restore the firmware default for this headless SITL process without saving it.
param set-default NAV_DLL_ACT 0
